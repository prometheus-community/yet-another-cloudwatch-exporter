apiVersion: v1alpha1
discovery:
  jobs:
    - type: AWS/EC2
      regions:
        - us-east-1
      roles:
        - {}
      metrics:
        - name: CPUUtilization
          statistics:
            - Average
          period: 300
          length: 300
      quotaMetrics: true
