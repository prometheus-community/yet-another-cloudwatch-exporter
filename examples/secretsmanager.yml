apiVersion: v1alpha1
discovery:
  jobs:
    - type: AWS/SecretsManager
      regions:
        - us-east-1
      length: 1800
      period: 1800
      metrics:
        - name: ResourceCount
          statistics: [Average]
