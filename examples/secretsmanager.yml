apiVersion: v1alpha1
discovery:
  jobs:
    - type: AWS/SecretsManager
      regions:
        - us-east-1
      period: 300
      length: 300
      metrics:
        - name: ResourceCount
          statistics: [Average]
